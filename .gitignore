# ==================== Android / Gradle ====================
.gradle/
# 本地隔离的 Gradle 家目录（依赖缓存 / wrapper 发行版），仅用于本机离线构建
.gradle-home/
build/
captures/
.externalNativeBuild/
.cxx/
local.properties
*.apk
*.aab
*.ap_
*.dex
*.class
output.json
release/

# 说明：Gradle Wrapper 的 gradle/wrapper/gradle-wrapper.jar 是官方要求入库的文件，
# 刻意不在此忽略，否则别人 clone 后无法执行 ./gradlew。

# ==================== IDE / 编辑器 ====================
.idea/
*.iml
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# ==================== Python ====================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# ==================== 密钥与本地配置 ====================
# 切勿把真实的 DashScope / DeepSeek API Key 提交上去
.env
.env.local
config.local.*

# ==================== 数据流水线的中间产物 ====================
# 标注缓存体积大且可重新生成（重跑不会重复消耗 API 额度），不入库。
# 最终数据集 / 评测集 / 报告**会**入库 —— 它们是流水线确实跑过的证据。
scene_ai_server/data/cache/
