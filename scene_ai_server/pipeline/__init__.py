"""离线流水线（数据引擎 → 蒸馏 → 量化 → 评测）。

设计原则：**零第三方依赖，只用标准库**。
    - API 调用用 urllib.request
    - 去重用自实现的 MinHash + LSH
    - 覆盖度用自实现的子模贪心（facility location，1-1/e 近似保证）
    - 配比用自实现的配额 + 质量最优填充

理由：数据引擎要在任何机器上都能立刻跑起来（包括没装 PyTorch 的机器），
而且这些算法本身就是项目要展示的"算法含量"，用现成库反而把重点藏起来了。
"""
