# ============================================================
# SCENE-AI 云端服务依赖清单
# 安装：pip install -r requirements.txt
# 运行：uvicorn app:app --host 0.0.0.0 --port 8000 --reload
# ============================================================

# ---------- Web 框架 ----------
fastapi>=0.110
uvicorn[standard]>=0.27
# ⚠️ FastAPI 接收 UploadFile（multipart/form-data）必须依赖 python-multipart，
#    漏装会在启动时报 "Form data requires python-multipart to be installed"
python-multipart>=0.0.9

# ---------- 数据校验 ----------
# app.py 使用了 list 等内建泛型标注，需要 Python 3.9+
pydantic>=2.5

# ---------- 调用 DashScope（阿里云百炼）兼容模式 API ----------
requests>=2.31

# ---------- PDF 文本抽取 ----------
pypdf>=4.0
