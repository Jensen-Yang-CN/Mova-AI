# ============================================================
# Mova-AI 离线流水线依赖（数据构造 / 蒸馏 / 量化 / 评测）
#
# 安装：pip install -r requirements-train.txt
# 说明：这些依赖体积大（PyTorch 数 GB），只在本机或训练服务器上安装，
#       在线服务不需要它们，因此与 requirements.txt 分开。
#
# ⚠️ 请按自己的 CUDA 版本安装 torch，例如：
#     pip install torch --index-url https://download.pytorch.org/whl/cu124
# ============================================================

torch>=2.3
transformers>=4.45
datasets>=2.20
accelerate>=0.33
peft>=0.12
trl>=0.10
safetensors>=0.4

# ---------- 量化 ----------
auto-gptq>=0.7 ; platform_system != "Windows"
awq>=0.2 ; platform_system != "Windows"
bitsandbytes>=0.43

# ---------- 数据引擎：去重 / 覆盖度优化 / 线性规划 ----------
scikit-learn>=1.4
sentence-transformers>=3.0
numpy>=1.26
scipy>=1.11
# 配比求解用的线性规划（也可用 scipy.optimize.linprog 替代）
pulp>=2.8

# ---------- 评测 ----------
pandas>=2.2
tqdm>=4.66

# ---------- 教师侧 logits 落盘（自托管推理，可选） ----------
# vllm>=0.6 ; platform_system == "Linux"
